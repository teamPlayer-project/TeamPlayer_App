<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.project.teamplayer">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".ApplicationClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme"
        tools:replace="android:supportsRtl">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivityGroup"
            android:label="@string/title_activity_maps_group"></activity>
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".no_activities1"
            android:parentActivityName=".select_action" />
        <activity android:name=".no_requests" />
        <activity android:name=".edit_activity" />
        <activity android:name=".participants_Items" />
        <activity
            android:name=".requestItem"
            android:label="@string/title_activity_request_item"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity_details"
            android:label="@string/title_activity_details"
            android:theme="@style/AppTheme" />
        <activity android:name=".sport_type" />
        <activity android:name=".city" />
        <activity android:name=".age_range" />
        <activity
            android:name=".registration"
            android:parentActivityName=".activity_Login" />
        <activity
            android:name=".activity_Search"
            android:parentActivityName=".select_action" />
        <activity
            android:name=".activity_create_new"
            android:parentActivityName=".select_action" />
        <activity
            android:name=".user_activities"
            android:parentActivityName=".select_action" />
        <activity
            android:name=".manager"
            android:parentActivityName=".select_action" />
        <activity
            android:name=".group"
            android:parentActivityName=".select_action" />
        <activity
            android:name=".select_action"
            android:parentActivityName=".activity_Login" />
        <activity
            android:name=".search_result"
            android:parentActivityName=".activity_Search" />
        <activity
            android:name=".requests"
            android:parentActivityName=".manager" />
        <activity
            android:name=".no_result"
            android:parentActivityName=".activity_Search" />
        <activity android:name=".chat" />
        <activity android:name=".activity_Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>